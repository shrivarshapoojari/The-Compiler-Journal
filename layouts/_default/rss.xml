{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title | plainify | htmlEscape }}{{ else }}{{ with .Title }}{{ . | plainify | htmlEscape }} on {{ end }}{{ .Site.Title | plainify | htmlEscape }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ printf "Recent content %s on %s" (cond (ne .Title .Site.Title) (printf "in %s " (.Title | plainify)) "") (.Site.Title | plainify) | htmlEscape }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Params.author.email }}
    <managingEditor>{{.}}{{ with $.Site.Params.author.name }} ({{ . | htmlEscape }}){{end}}</managingEditor>{{end}}{{ with .Site.Params.author.email }}
    <webMaster>{{.}}{{ with $.Site.Params.author.name }} ({{ . | htmlEscape }}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{ . | htmlEscape }}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{- range $pages -}}
    <item>
      <title>{{ .Title | plainify | htmlEscape }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Params.author.email }}<author>{{.}}{{ with $.Site.Params.author.name }} ({{ . | htmlEscape }}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Summary | plainify | htmlEscape }}</description>
      {{- if .Content }}
      <content:encoded>{{ .Content | htmlEscape }}</content:encoded>
      {{- end }}
      {{- range .Params.tags }}
      <category>{{ . | htmlEscape }}</category>
      {{- end }}
    </item>
    {{- end }}
  </channel>
</rss>
